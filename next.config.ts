/** @type {import('next').NextConfig} */
const nextConfig = {
  // Здесь могут быть ваши другие настройки, например, `reactStrictMode: true`
  // Оставьте их, если они есть.
};

export default nextConfig;